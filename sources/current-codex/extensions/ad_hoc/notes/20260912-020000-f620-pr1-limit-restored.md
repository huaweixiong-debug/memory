# F620 程序 1 泄漏上限恢复

- 2026-09-12：用户确认可修改后，在真实 B 工位 COM6、从站 1 上将程序 1 的 `Test FAIL` 从 10.04 恢复为 `1.004 mL/min`（原始固定小数值 1004）。
- Modbus 读回 `1004`，单位仍为 `mL/min`；恢复 UI 后日志记录 `BOOT ATEQ_OK port=COM6 slave=1` 和 `ATEQ_PROGRAM_SYNC`。未启动测试周期。
