# F620 程序 1 实测泄漏上限

- 2026-09-12：在远程电脑真实 COM6、从站 1 上，选择编辑程序 1 后只读查询到 `Test FAIL=1004`，按 F620 的 x1000 固定小数换算为 `1.004 mL/min`。
- 同次查询得到 `LeakUnit=51000`（mL/min），`Ref. FAIL=0`，表示当前未启用下限判定；查询结束后已恢复远程 UI，日志重新出现 `BOOT ATEQ_OK` 与 `ATEQ_PROGRAM_SYNC`。
