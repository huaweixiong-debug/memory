# ATEQ M16.1 启动链路修复

- 2026-09-11：确认 `M16.1` 是 PLC→UI 的 B 工位生产启动上升沿，UI 不写入或清除该位。
- `StationController` 在收到该链路的测试请求后调用 ATEQ Modbus RTU `0x0001` 启动线圈（功能码 `0x05`，`FF00`），再由 `SerialAteq.run()` 监控 `StepCode 4/5/6`，结束兼容 `65525/65535`，只有完整结果才允许落库和打印。
- 远程 UI 已重启，`COM6/从站1` 与 PLC `192.168.2.1:102` 的连接验证通过；未在未确认工件状态时自动触发实体测试。扫码枪 TCP 9004 仍需现场设备端开启 Server。
