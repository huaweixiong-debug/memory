# F620 程序 1 临时泄漏上限与 UI 验证

- 2026-09-12：按用户要求，为单个合格样件测试，将真实 B 工位 COM6/从站 1/程序 1 的 `Test FAIL` 从 1.004 调到 10.04 mL/min；Modbus 读回原始值 10040，单位代码仍为 mL/min。
- UI 重启后 `BOOT ATEQ_OK port=COM6 slave=1`、`ATEQ_PROGRAM_SYNC station=B program=1` 成功，扫码枪 TCP 9004 也为 ESTABLISHED，PID 与 UI 一致；未触发测试。
