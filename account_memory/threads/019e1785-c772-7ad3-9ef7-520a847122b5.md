# codex现在用了什么大模型？解读下toml文档

- 会话 ID：`019e1785-c772-7ad3-9ef7-520a847122b5`
- 主题：Codex、模型与开发工具
- 创建：2026-05-11T22:51:43+08:00
- 更新：2026-05-11T22:54:23+08:00
- 工作目录：`\\?\D:\Codex`
- 用户消息数：3；rollout 行数：38
- 原始记录：`C:\Users\Administrator\.codex\sessions\2026\05\11\rollout-2026-05-11T22-51-43-019e1785-c772-7ad3-9ef7-520a847122b5.jsonl`

## 用户请求

### 用户消息 1

# Files mentioned by the user:

## config.toml: D:/Codex/config.toml

## My request for Codex:
codex现在用了什么大模型？解读下toml文档

### 用户消息 2

现在是什么模型

### 用户消息 3

在模型选择里切换，没有用吗？

## 助手结果摘录

### 助手结果摘录 1

I'll read the config.toml file to understand the current model configuration.

### 助手结果摘录 2

## Codex 当前大模型配置解读

### 核心模型配置

你的 Codex 当前使用 **DeepSeek V4 Pro**，通过本地兼容代理运行：

| 配置项 | 值 | 含义 |
|---|---|---|
| `model_provider` | `deepseek-codex` | 自定义模型提供商 |
| `model` | `deepseek-v4-pro` | 当前使用的模型 |
| `model_reasoning_effort` | `high` | 高推理努力（深度思考） |
| `base_url` | `http://127.0.0.1:17777/v1` | 本地 Codex 兼容代理地址 |
| `wire_api` | `responses` | 使用 Responses API 协议 |
| `requires_openai_auth` | `true` | 需要 OpenAI 认证 |

### 关键解读

- **你不是在直连 OpenAI 的模型**，而是通过本地的兼容代理（`127.0.0.1:17777`）来访问 DeepSeek V4 Pro。这个代理将 OpenAI 风格的 API 请求转发到 DeepSeek，所以 Codex 可以无缝使用。
- **推理努力设为 `high`**，意味着模型会花更多计算资源做深度推理，适合复杂编程任务。
- **禁用了响应存储**（`disable_response_storage = true`），对话不会持久化到云端。

### 其他配置

| 配置区 | 用途 |
|---|---|
| `[marketplaces]` | 两个插件市场：OpenAI 官方捆绑包 + 主运行时 |
| `[plugins]` | 启用了 **Documents**、**Spreadsheets**、**Presentations**、**GitHub**、**Browser** 五个插件 |
| `[tui.model_availability_nux]` | TUI 界面中 `gpt-5.5` 和 `deepseek-v4-pro` 对用户可见 |
| `[mcp_servers]` | 接入了 OpenAI 开发者文档 MCP |
| `[windows]` | 沙箱模式设为 `elevated`（提权模式） |
| `[projects]` | 信任了一个远程 UNC 路径项目：`\\100.94.24.38\ateq tester\leak test` |

### 总结

> **Codex 运行在 DeepSeek V4 Pro 上**，通过本地 `127.0.0.1:17777` 的兼容代理桥接，协议走的是 OpenAI Responses API。这是一个高推理模式的自定义部署方案。

---

如果你需要调整模型（比如切换到 `gpt-5.5` 或降低推理努力），或者想修改代理地址，告诉我即可。

### 助手结果摘录 3

我是 **DeepSeek V4 Pro**，通过你本地的 Codex 兼容代理（`127.0.0.1:17777`）在运行，推理努力设为 `high`。

就是 `config.toml` 里配的那个 `deepseek-v4-pro`。

### 助手结果摘录 4

你说的“模型选择里切换”指的是 TUI 界面里的模型选择器吗？

看了一下你的 `config.toml`，`[tui.model_availability_nux]` 段里 `gpt-5.5` 和 `deepseek-v4-pro` 都被标记为可见（值=1），所以理论上你在界面里应该能看到两个选项。

但实际上有几个原因可能导致切换“没生效”：

1. **本地代理只转发 DeepSeek**：你的 `base_url` 指向 `127.0.0.1:17777`，这个兼容代理可能只配置了转发到 DeepSeek，不管界面选什么模型，最终都落到同一个后端。界面选的只是请求里的 `model` 字段，代理怎么处理这个字段取决于代理本身的配置。

2. **OAuth 锁定了模型**：`requires_openai_auth = true` + `preferred_auth_method = "oauth"`，可能意味着模型由认证令牌决定，界面切换被忽略。

3. **配置文件未被重读**：Codex 可能在启动时读一次 `config.toml`，界面切换如果只是存在内存里，重启后可能就回到文件里写的 `deepseek-v4-pro`。

---

简单说，**界面切换只是改请求参数，你的本地代理才是实际决定用哪个模型的**。如果代理没有配 GPT-5.5 的路由，切换就只是改了请求里的名字而已，后端不会变。

要不要我帮你看看本地代理那边的配置，确认它到底支持哪些模型？

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
