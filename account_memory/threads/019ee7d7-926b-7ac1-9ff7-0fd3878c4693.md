# 为什么这里的settings里coding里没有连接选项？让我可以ssh到其他电脑？

- 会话 ID：`019ee7d7-926b-7ac1-9ff7-0fd3878c4693`
- 主题：远程、网络与 NAS
- 创建：2026-06-21T09:42:05+08:00
- 更新：2026-06-21T09:56:16+08:00
- 工作目录：`\\?\D:\Codex`
- 用户消息数：2；rollout 行数：236
- 原始记录：`C:\Users\Administrator\.codex\sessions\2026\06\21\rollout-2026-06-21T09-42-05-019ee7d7-926b-7ac1-9ff7-0fd3878c4693.jsonl`

## 用户请求

### 用户消息 1

为什么这里的settings里coding里没有连接选项？让我可以ssh到其他电脑？

### 用户消息 2

Codex 的 settings 里的 coding 区域怎么没有连接选项，可以设置ssh的，我在其他电脑里的codex就有

## 助手结果摘录

### 助手结果摘录 1

日志已经给出关键原因：Codex 启动时访问 `ab.chatgpt.com/v1/initialize` 超时，Statsig 功能开关没有成功下发。SSH 连接入口很可能因此回退为隐藏，而不是本机不支持 SSH。接下来确认代理是否放行该域名及当前应用版本。

### 助手结果摘录 2

代理差异也确认了：`100.82.136.106:17890` 可达，但只写在 `C:\Users\Administrator\.codex\.env`；Windows 系统代理当前关闭。桌面前端的 Statsig 请求没有走这份 CLI 代理，所以功能开关请求超时。

### 助手结果摘录 3

这里还有第二个因素：本机的桌面包记录显示是 Windows Store 构建 `26.519.80914`，而 CLI 已更新到 `0.130.0-alpha.5`。更新 CLI 不等于更新 Settings 界面，SSH 入口由桌面包和服务端开关共同决定。

### 助手结果摘录 4

又发现本机并不是单纯启动官方 Codex：当前还有 `Codex++` 和它的管理进程，开始菜单里也只有 Codex++。这可能把官方客户端固定到某个本地版本或改变网络代理，必须把它和另一台电脑的启动方式区分开。

### 助手结果摘录 5

你说得对，Codex 确实有 SSH“连接”入口。这台电脑缺失主要有两个原因：

1. Codex 访问 `ab.chatgpt.com` 的功能开关服务持续超时，SSH 入口可能因此被隐藏。
2. 代理只配置在 [`.env`](C:/Users/Administrator/.codex/.env)，它主要影响 CLI；Windows 系统代理目前关闭，桌面界面没有走代理。
3. 本机还通过 `Codex++` 启动，并残留多套 Codex 组件。更新 CLI 不等于更新桌面 Settings。

建议这样处理：

- 在代理软件中开启“系统代理”或 TUN 模式，地址为 `100.82.136.106:17890`。
- 确保 `ab.chatgpt.com`、`chatgpt.com`、`chat.openai.com` 可以访问。
- 完全退出 Codex 和 Codex++ 后，更新并直接启动官方 Codex，再重新登录。
- 如果同一账号仍没有入口，才属于服务端灰度或两台电脑版本不同，无法通过 `config.toml` 强制开启。

本机状态里已经有 `remote-project-connection-backfill-completed`，说明客户端包含远程项目相关代码，问题更像功能开关未成功下发。

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
