# 实现delta坐标手动控制

- 会话 ID：`019dc1f9-ed92-7f22-9759-994b7129c7cc`
- 主题：视觉检测与 YOLO
- 创建：2026-04-25T08:11:15+08:00
- 更新：2026-04-25T08:25:00+08:00
- 工作目录：`\\?\UNC\100.82.136.106\Work\奥特佳\006 定排量缸体螺栓扭紧\视觉分拣案例`
- 用户消息数：1；rollout 行数：234
- 原始记录：`C:\Users\Administrator\.codex\sessions\2026\04\25\rollout-2026-04-25T08-11-15-019dc1f9-ed92-7f22-9759-994b7129c7cc.jsonl`

## 用户请求

### 用户消息 1

你看下 delta机械臂是如何工作的，我想先手动给他坐标，让它工作。

## 助手结果摘录

### 助手结果摘录 1

我准备给上位机加一个很小的“手动坐标”入口：选择相对/绝对模式，填 X/Y/Z 和可选速度 F，点按钮后直接发送 `G91/G90 + G0 ...`。这样不碰自动识别逻辑，也不会自动触发吸盘和投放流程。

### 助手结果摘录 2

补丁没套上，是因为我看到的源码片段经过控制台编码显示成了乱码，实际文件里的中文字符串不同。我先用 UTF-8 输出把这段重新读准，再贴合真实文本打补丁。

### 助手结果摘录 3

我已经把入口加进 `CameraDetect.py` 了：默认相对移动，能填 X/Y/Z 和 F 速度，发送前会校验串口已打开、坐标是数字。现在做一次源码片段复核和 Python 语法编译检查。

### 助手结果摘录 4

语法编译通过了，新增代码也在预期位置。最后我取一下关键行号，方便你之后直接定位到“手动坐标”入口。

### 助手结果摘录 5

我看完了。它的链路是：上位机 `CameraDetect.py` 通过串口 `115200` 发送 G-code，ESP32 在 `command.cpp` 解析 `G0/G1 X/Y/Z/F`，再由 `interpolation.cpp` 做插补，`robotGeometry.cpp` 把末端坐标反解成 Delta 三个电机角度，最后主循环驱动步进电机。

我已经给上位机加了“手动坐标(mm)”区域：  
[CameraDetect.py](<Y:\奥特佳\006 定排量缸体螺栓扭紧\视觉分拣案例\2025年11月17日-Delta机械臂上下位机\2025年11月17日-Delta机械臂上下位机\pc\CameraDetect.py:390>)

现在界面里可以选 `相对/绝对`，填 `X/Y/Z` 和可选 `F速度`，点“发送坐标”会发类似：

```text
G91
G0 X5 Y0 Z0 F20
```

建议第一次调试这样来：

1. 打开上位机，选择机械臂串口，点“打开串口”。
2. 点“激活电机”。
3. 手动坐标先选 `相对`。
4. 先小步试，比如 `X=2`、`Y=2`、`Z=-2`，速度 `F=20`。
5. 确认方向和高度没问题后再加大距离。

我也做了语法检查：`python -m py_compile CameraDetect.py` 通过。注意一点：下位机源码里 `interpolation.cpp` 的限位最后被强制 `retVal = true`，所以手动坐标一定先小步慢速试，尤其不要随手发绝对坐标 `Z=0` 这种值。

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
