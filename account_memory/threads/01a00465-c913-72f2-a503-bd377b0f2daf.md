# 用5.6 luna实施此计划，然后用5.6 sol审核。

- 会话 ID：`01a00465-c913-72f2-a503-bd377b0f2daf`
- 主题：工业设备与自动化项目
- 创建：2026-08-15T15:49:34+08:00
- 更新：2026-08-16T13:33:43+08:00
- 工作目录：`Y:\协众\014 武汉RAD干检第二台\Leak Test 2 Channels_V0.0`
- 用户消息数：2；rollout 行数：3661
- 原始记录：`\\?\C:\Users\Administrator\.codex\sessions\2026\08\15\rollout-2026-08-15T15-49-34-01a00465-c913-72f2-a503-bd377b0f2daf.jsonl`

## 用户请求

### 用户消息 1

用5.6 luna实施此计划，然后用5.6 sol审核。

### 用户消息 2

PLEASE IMPLEMENT THIS PLAN:
# Luna 实施、Sol 审核执行计划

## 目标与职责

- 将 `Main.vi` 完整重写为 Python 桌面程序，保持双工位检测、PLC、ATEQ、扫码、数据库、标签、校准、查询、权限和手动功能不变。
- 界面升级为 Windows 11 风格，但保留“测试、设置、查询、手动”四页、A/B 工位布局、现有中英法术语和操作顺序。
- 所有代码修改、测试、打包和问题修复由 `gpt-5.6-luna` 完成。
- `gpt-5.6-sol` 只读审核 Luna 的交付物，不直接修改代码；发现 P0/P1 后退回 Luna 修复，再由 Sol 复审。
- 当前仍为规划模式，尚未修改项目文件；进入执行模式后才开始实施。

## Luna 实施批次

1. **基线与协议冻结**
   - 只读分析原 LabVIEW 项目、`Main.vi`、子 VI、`Setup.ini`、条码及校准脚本。
   - 建立功能等价矩阵、PLC 点表、ATEQ 协议表、数据库结构、状态转换表和操作员流程。
   - PLC 点表必须包含地址、位、方向、有效电平、脉冲时间、清除方、唯一写入方及同字节所有权。
   - 读取 `SHOW CREATE TABLE info_A/info_B`，确认唯一主键；没有可靠唯一键时阻止生产写库方案进入 live。
   - 未确认的 PLC 时序、ATEQ 命令、标签回执、校准流程不得通过推测补齐。

2. **Python 基础工程**
   - 仅在项目下新建 `python_app`，不修改原 `.vi/.lvproj`、PLC 程序及生产数据库。
   - 固定 Python 3.10、PySide6、python-snap7、pyserial、PyMySQL、cryptography、pytest、PyInstaller。
   - 锁定依赖版本和哈希，建立离线 wheelhouse。
   - 默认 `SIMULATE`；运行模式由统一 `CapabilityPolicy` 强制限制，运行中不可切换。
   - 日志、缓存、周期日志写入 `%ProgramData%\LeakTest2Channels`；开发及 shadow 阶段只读访问 `D:\data`。
   - 数据库密码: <REDACTED>

3. **设备与服务适配器**
   - PLC：M 区批量读取、唯一写队列、回读确认、断线保护和 Fake PLC。
   - 混合所有权的 M 字节不得直接整字节读改写；必须验证 Snap7 位写的原子性，或先调整 PLC 握手映射。
   - PLC 轮询周期必须小于最短有效脉冲的三分之一，并记录 p99 抖动。
   - ATEQ：COM6、COM7 各自使用独立 actor，同一物理端口只允许一个实例；实现部分帧、粘帧、旧帧、CRC、超时和重连处理。
   - 每条 ATEQ 结果绑定工位、`cycle_id`、请求序号和程序号，未知状态不得判为 OK。
   - 扫码器、条码规则、校准顺序、MySQL、BarTender 和许可证分别通过接口与 Fake 实现隔离。
   - B2–B6 可并行实现接口、Fake、解析器和单元测试；真实适配器必须等待对应协议基线确认。

4. **运行时与持久化**
   - A/B 使用完全独立的状态机、扫码数据、ATEQ 会话、数据库记录和故障状态。
   - 状态至少包括 `IDLE、WAIT_SCAN、READY、TEST_1、WAIT_2、TEST_2、LABELING、COMPLETE、CALIBRATION、FAULT`。
   - 每次转换固定输入事件、守卫条件、副作用、超时、持久化点、取消动作和恢复动作。
   - 建立持久化 `CycleJournal`，记录 `cycle_id`、工位、序列号、MySQL 行 ID、阶段提交状态、打印意图和打印结果。
   - 阶段二只能按工位表和稳定主键更新，不允许仅按可能重复的序列号更新。
   - 打印流程固定为：保存 `PRINT_INTENT` →
...[内容已截断]

## 助手结果摘录

### 助手结果摘录 1

表头和 1366 底部布局已完成闭合；已将根目录坏 EXE 与嵌套重复 EXE 隔离，canonical one-folder 包已由 dist_ui7 重建。目前只在收尾 canonical 诊断/双工位 smoke、最终哈希与文件计数核验。

### 助手结果摘录 2

最终闭合已完成：

- 表头：主/查询表使用多语言双行短表头、固定高度、禁用省略、最小列宽；1920×1080、1366×768 及 125%/150% DPI 均可读。
- 底部：四类原始指示项采用紧凑换行标签，1366 初始视图中可见且不截断。
- 包清理：`python_app/package_dist_final` 仅保留一个 canonical one-folder 包：
  `package_dist_final/LeakTest2Channels/LeakTest2Channels.exe`
  - SHA-256：`C5C070872668DF4DF513885E726B645144A43ADDBA610948157052BBC75B916D`
  - 根目录损坏 EXE 和嵌套重复 EXE 已移至临时隔离目录，未删除。
- 验证：
  - `pytest -q`：45 passed
  - `compileall`：0
  - source simulate diagnose：0
  - source smoke cycle：0，`stations=2 records=2 labels=2`
  - canonical packaged diagnose：0
  - canonical packaged smoke：0
  - shadow/live：均按设计返回 2（阻断，无真实连接）
  - package 内 EXE 数：1
- 截图尺寸：
  - Main：1920×1080、1366×768
  - Setup/Query/Manual：1920×1080
  - DPI：2400×1350、2880×1620

已更新 `review_package/00_change_summary.md`、`07_test_report.md`、`13_sol_closure_matrix.md`、`14_ui_screenshots.md`、`candidate_hashes.sha256` 和 `source_manifest.txt`。

### 助手结果摘录 3

我会做最小范围布局修复：仅调整底部指示标签宽度/布局约束，补充 1366/1920 几何回归断言，再重生成截图、哈希并执行完整验证；不会触碰功能或安全门禁。

### 助手结果摘录 4

当前未卡在打包；已定位为底部四组横向 LED+文字导致 footer 最小宽度在 1366 被 Qt 强制放大。正在将每个指示项内部改为紧凑纵向 LED/文字布局，保留四类层级与读回功能，同时让整体窗口仍能实际缩放到 1366×768。

### 助手结果摘录 5

当前已完成最小布局修复：指示项改为紧凑纵向 LED/文字，四类 footer 标签在 1366/1920 实测宽度均不小于 `sizeHint`，窗口可实际缩放到 1366×768；定向 UI 测试已 6 passed。截图已重生成，尚剩文档/hash 更新、全套 pytest/compile、source/canonical diagnose/smoke 和 shadow/live 阻断核验。先检查无残留打包进程。

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
