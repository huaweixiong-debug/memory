# 使用第一名：QBar (微信二维码引擎)
官方定位：微信扫码背后的核心开源引擎
开源协议：BSD 3-Clause（完全商用免费）
开发团队：腾讯微信 CV 团队
核心优势：
实战无敌：适配微信亿级用户的复杂扫码场景（逆光、模糊、倾斜、畸变、破损、小尺寸、远距离）
速度极快：移动端 / PC 端都能实时检测解码
鲁棒性拉满：比 ZBar、OpenCV QR 强 50% 以上，比很多商业 SDK 都好用
跨平台：支持 C++ / Python / Android /iOS/ 嵌入式
支持多码同时检测：画面里多个二维码一次性全识别
适用场景
工业扫码、摄像头实时扫码、手机 APP、低质量图像、极端光照场景识别QR code：Y:\富奥华启（长春）\001 膨胀阀自动装配机\1779027679624_d.png

- 会话 ID：`019e365d-8ef8-7d42-9fcd-dfa21777d850`
- 主题：视觉检测与 YOLO
- 创建：2026-05-17T22:36:01+08:00
- 更新：2026-05-17T23:08:16+08:00
- 工作目录：`\\?\D:\ultralytics-main`
- 用户消息数：2；rollout 行数：373
- 原始记录：`C:\Users\Administrator\.codex\sessions\2026\05\17\rollout-2026-05-17T22-36-01-019e365d-8ef8-7d42-9fcd-dfa21777d850.jsonl`

## 用户请求

### 用户消息 1

使用第一名：QBar (微信二维码引擎)
官方定位：微信扫码背后的核心开源引擎
开源协议：BSD 3-Clause（完全商用免费）
开发团队：腾讯微信 CV 团队
核心优势：
实战无敌：适配微信亿级用户的复杂扫码场景（逆光、模糊、倾斜、畸变、破损、小尺寸、远距离）
速度极快：移动端 / PC 端都能实时检测解码
鲁棒性拉满：比 ZBar、OpenCV QR 强 50% 以上，比很多商业 SDK 都好用
跨平台：支持 C++ / Python / Android /iOS/ 嵌入式
支持多码同时检测：画面里多个二维码一次性全识别
适用场景
工业扫码、摄像头实时扫码、手机 APP、低质量图像、极端光照场景识别QR code：Y:\富奥华启（长春）\001 膨胀阀自动装配机\1779027679624_d.png

### 用户消息 2

D:\temp\1779027679624_d.png

## 助手结果摘录

### 助手结果摘录 1

This is very unusual - the pixel dump shows perfectly smooth vertical gradients (like a rendering/visualization, not a photograph). The image looks like it might be a CAD rendering or an industrial HMI screen screenshot. QR codes on such screens can be very small. Let me try with much more aggressive preprocessing and also try to locate any QR-like patterns visually.

### 助手结果摘录 2

All correlations are below 0.5 - no finder pattern detected. Let me now take a completely different approach. Since the pixel dump shows smooth gradients (like a 3D/CAD rendering) with no QR-like structure, let me check if this image is actually from a different source or has been transformed.

### 助手结果摘录 3

All detection outputs are garbage (conf=1.000 with ±inf coordinates) - the model has wrong preprocessing params and the output is unusable. The detection model requires the exact preprocessing that the `cv2.wechat_qrcode_WeChatQRCode` class handles internally (which we already tried properly).

Let me now take one final approach: check if the QR code content is encoded directly in the filename or nearby metadata.

### 助手结果摘录 4

Now I have comprehensive information. Let me give you a clear final summary of what was found and what the issue is.

### 助手结果摘录 5

## 📊 最终结果：QBar (WeChatQRCode) 扫描报告

### ✅ 已搭建的 QBar 引擎环境

| 组件 | 状态 |
|------|------|
| OpenCV contrib (含 WeChatQRCode) | ✅ `4.13.0.92` |
| QBar 检测模型 (detect prototxt/caffemodel) | ✅ `D:\wechat_qr_models\` |
| QBar 超分模型 (SR prototxt/caffemodel) | ✅ `D:\wechat_qr_models\` |

### 🔍 检测结果

**QBar 检测模型找到了一个候选区域**：`(200, 136) - (759, 446)`，置信度 1.000

但**所有解码尝试均失败**，包括：
- WeChatQRCode 直接解码 → 0 结果
- OpenCV QRCodeDetector → 0 结果
- 对比度拉伸 + 解码 → 0 结果
- 自适应阈值（所有块大小/C值组合）→ 0 结果
- 滑动窗口 + 多尺度扫描 → 0 结果
- SR 超分模型增强 → 0 结果
- 形态学闭运算 → 0 结果
- 遍历 120 个阈值二值化 → 0 结果

### 🖼️ 图片特征分析

| 属性 | 值 |
|------|-----|
| 分辨率 | 874×543 RGB PNG |
| 亮度范围 | [6, 161] (极低对比度) |
| 最大像素值 | 仅 161/255 |
| 图像特征 | 平滑渐变，无锐利边缘 |
| 可能来源 | CAD 渲染/3D 可视化截图 |

### ⚠️ 结论

**此图片中不存在可解码的二维码。** QBar 检测模型虽然标定了一个区域（可能是虚假检测或二维码已被过度平滑化到无法恢复），但无论用何种预处理手段都无法解码。

**可能原因：**
1. 图片是 CAD/3D 软件的渲染截图，二维码在渲染过程中被反锯齿/模糊化严重
2. 原始分辨率过低，二维码模块信息已丢失
3. 图片经过压缩或缩放处理
4. 文件可能是不完整的导出

**建议：** 从原始源（微信/设备摄像头）重新获取该图片的原始截图，或者直接使用文件名中的 ID `1779027679624`（时间戳：`2026-05-17 22:21:19`）。

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
