# 我映射了X：和U：但是文件资源管理器里看不到

- 会话 ID：`019e0775-fdba-76c0-87ce-397566ddcb7e`
- 主题：Windows、系统与硬件
- 创建：2026-05-08T20:00:33+08:00
- 更新：2026-05-08T20:48:20+08:00
- 工作目录：`\\?\D:\Claude`
- 用户消息数：4；rollout 行数：283
- 原始记录：`C:\Users\Administrator\.codex\sessions\2026\05\08\rollout-2026-05-08T20-00-33-019e0775-fdba-76c0-87ce-397566ddcb7e.jsonl`

## 用户请求

### 用户消息 1

我映射了X：和U：但是文件资源管理器里看不到

### 用户消息 2

yes please restart

### 用户消息 3

我先建了x盘，后来在文件资源管理器里找不到，我又建了u盘，发现还是找不到，就来找你了

### 用户消息 4

文件资源管理器
Windows 找不到 "u:\"。请检查拼写并重试。 
确定

## 助手结果摘录

### 助手结果摘录 1

找到原因了！当前 Shell 是以 **管理员身份** 运行的（elevated），所以 `net use` 创建的映射存在于**管理员会话**中。但你打开的文件资源管理器是**普通用户身份**运行的，它看不到管理员会话里的映射。

这就是为什么命令行里能访问 X: 和 U:，但文件资源管理器却说"找不到"。

**解决方法：**

### 助手结果摘录 2

在非管理员环境下重新映射。让我用一个脚本来完成：

### 助手结果摘录 3

两个映射都在管理员会话里。需要先删掉，然后用非管理员身份重新建。让我用一个非管理员进程来同时完成"删旧 + 建新"：

### 助手结果摘录 4

## 问题原因找到了

你的 Shell 是**以管理员身份运行的**（elevated），`net use` 创建的映射在**管理员会话**中。但文件资源管理器是**普通用户身份**运行的，它看不到管理员会话里的映射。这就是为什么：
- 命令行里能访问 X: 和 U: ✅
- 文件资源管理器显示"找不到" ❌

让我用非管理员进程来重新映射：

### 助手结果摘录 5

X: 和 U: 已经通过**非管理员身份**重新映射了。现在回到文件资源管理器，按一下 `F5` 刷新，或者在地址栏输入 `X:\` 回车试试看。

这次文件资源管理器应该能看到了，因为映射是在和文件资源管理器**相同的用户上下文**中创建的。

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
