# I'm investigating a "memory/amnesia" problem in the Hermes Agent desktop (TUI) client. Users say it forgets context and is incoherent.

Looking at `/home/dell/.hermes/hermes-agent/tui_gateway/server.py`:

1. In `_compress_session_history` (around line 490), find the full function definition and explain: when is it called, what triggers context compression, and what could cause it to compress TOO aggressively (too frequently, or dropping too many messages)?

2. Look at `prompt.submit` method - how does it use history? Does it properly maintain conversation context between turns?

3. Look at `_init_session` - what does it set for `history` initially?

4. Look at `agent/memory_manager.py` and `agent/memory_provider.py` - what role do they play in maintaining conversation memory? Do they have any bugs that could cause memory loss?

5. Look at `gateway/hooks.py` - search for any middleware that affects context/message history. Does it prune messages?

6. Also check `plugins/memory/` directory - is there a memory plugin that could be involved?

7. Look at `_cached_system_prompt` usage in the codebase - is it possible that a stale cached system prompt is being used?

8. Check if there's any async/race condition in how `session["history"]` is accessed between the agent thread (in prompt.submit) and the UI thread.

Focus on identifying root causes for why the desktop client might appear to "forget" earlier conversation context. Look for:
- Aggressive/incorrect compression
- History being reset/cleared unexpectedly
- Race conditions between threads accessing history
- Session DB not properly persisting/restoring messages
- Context window/cache mismanagement

Report back with concrete code locations (file:line) and explanation for each potential root cause.

- 会话 ID：`019de9b8-3dc6-7293-9537-8cebd57199e6`
- 主题：远程、网络与 NAS
- 创建：2026-05-03T01:24:19+08:00
- 更新：2026-05-03T01:59:54+08:00
- 工作目录：`D:\Claude`
- 用户消息数：1；rollout 行数：484
- 原始记录：`C:\Users\Administrator\.codex\sessions\2026\05\03\rollout-2026-05-03T01-24-19-019de9b8-3dc6-7293-9537-8cebd57199e6.jsonl`

## 用户请求

### 用户消息 1

I'm investigating a "memory/amnesia" problem in the Hermes Agent desktop (TUI) client. Users say it forgets context and is incoherent.

Looking at `/home/dell/.hermes/hermes-agent/tui_gateway/server.py`:

1. In `_compress_session_history` (around line 490), find the full function definition and explain: when is it called, what triggers context compression, and what could cause it to compress TOO aggressively (too frequently, or dropping too many messages)?

2. Look at `prompt.submit` method - how does it use history? Does it properly maintain conversation context between turns?

3. Look at `_init_session` - what does it set for `history` initially?

4. Look at `agent/memory_manager.py` and `agent/memory_provider.py` - what role do they play in maintaining conversation memory? Do they have any bugs that could cause memory loss?

5. Look at `gateway/hooks.py` - search for any middleware that affects context/message history. Does it prune messages?

6. Also check `plugins/memory/` directory - is there a memory plugin that could be involved?

7. Look at `_cached_system_prompt` usage in the codebase - is it possible that a stale cached system prompt is being used?

8. Check if there's any async/race condition in how `session["history"]` is accessed between the agent thread (in prompt.submit) and the UI thread.

Focus on identifying root causes for why the desktop client might appear to "forget" earlier conversation context. Look for:
- Aggressive/incorrect compression
- History being reset/cleared unexpectedly
- Race conditions between threads accessing history
- Session DB not properly persisting/restoring messages
- Context window/cache mismanagement

Report back with concrete code locations (file:line) and explanation for each potential root cause.

## 助手结果摘录

### 助手结果摘录 1

Now let me also check the `gateway/hooks.py` and the `session_context` for history pruning, and then look at how `prompt.submit` in the TUI handles its version check:

### 助手结果摘录 2

Now let me check a few more critical areas — the `_flush_messages_to_session_db` in run_agent.py and the `plugins/memory/__init__.py`:

### 助手结果摘录 3

Now I have enough information to synthesize the findings. Let me check one last thing — the `_cached_system_prompt` caching logic in `run_agent.py`:

### 助手结果摘录 4

Now let me look at specific compression thresholds and check one more critical path:

### 助手结果摘录 5

Here is my comprehensive analysis of the memory/amnesia problem in the Hermes Agent TUI client.

---

## Root Cause Analysis: TUI Memory/Amnesia

### 1. `_compress_session_history` — Aggressive Compression

**Location:** `tui_gateway/server.py:488-501`

```python
def _compress_session_history(session: dict, focus_topic: str | None = None) -> tuple[int, dict]:
    from agent.model_metadata import estimate_messages_tokens_rough
    agent = session["agent"]
    history = list(session.get("history", []))
    if len(history) < 4:
        return 0, _get_usage(agent)
    approx_tokens = estimate_messages_tokens_rough(history)
    compressed, _ = agent._compress_context(
        history,
        getattr(agent, "_cached_system_prompt", "") or "",
        approx_tokens=approx_tokens,
        focus_topic=focus_topic or None,
    )
    session["history"] = compressed
    session["history_version"] = int(session.get("history_version", 0)) + 1
    return len(history) - len(compressed), _get_usage(agent)
```

**How it's called:**
- Manual: via `session.compress` method (`server.py:1213-1225`) when user types `/compress`
- Internal: The agent's `run_conversation` also calls `_compress_context` automatically at **multiple trigger points** (see point 2).

**Root cause — Aggressive compression:**
The `estimate_messages_tokens_rough` function (`agent/model_m
...[内容已截断]

> 说明：这是用于检索的 memory 卡片，不替代原始聊天记录；敏感凭据已脱敏，完整记录仍保留在原始 rollout 文件中。
